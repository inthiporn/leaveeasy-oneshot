# test-results.md — ผลการรันเทสต์ซ้ำ (regression) หลังแก้บั๊กรอบ Checkpoint 2

> รันจริงผ่านเบราว์เซอร์ (Claude Browser) กับเซิร์ฟเวอร์ local `http://localhost:3002` (`serve.ps1`)
> เทียบผลกับข้อมูลจริงใน Firestore project `leaveeasy-inthiporn` (production) ทุกครั้งที่ระบุ "ยืนยันระดับฐานข้อมูล"
> ช่วงเวลารัน: **2026-09-20 12:45 – 12:49 น. (UTC+7)**
> บัญชีทดสอบทั้งหมดที่สร้างระหว่างรัน (regress1-20260920@…, regress2-20260920@…) และข้อมูลใบลาที่สร้างขึ้น **ถูกลบออกจาก production หมดแล้วหลังเทสต์เสร็จ**

---

## สรุปผล

| # | ชื่อเทสต์ | ผล | เวลาที่รัน |
|---|---|---|---|
| 1 | ไม่มี `dashboard.html` — role ที่ไม่ใช่ hr เข้าตรง ๆ ต้องถูกเด้งกลับ | ✅ ผ่าน | 12:46 |
| 2 | `leave-types.html` เข้าได้แม้ไม่ใช่ hr — role ที่ไม่ใช่ hr เข้าตรง ๆ ต้องถูกเด้งกลับ | ✅ ผ่าน | 12:46 |
| 3 | Logout แล้วข้อมูลเก่าค้างจอ — ต้องเด้งไป login ทันทีไม่ต้อง refresh | ✅ ผ่าน | 12:46 |
| 4 | Validation: เว้นช่องหัวข้อว่างแล้วกดบันทึก ต้องไม่บันทึกและมีข้อความเตือน | ✅ ผ่าน | 12:47 |
| 5 | ความปลอดภัย: ไม่ล็อกอินแล้วเปิดหน้ารายการ ต้องอ่านข้อมูลไม่ได้ | ✅ ผ่าน | 12:45 |
| 6 | ความปลอดภัย: ล็อกอินบัญชีที่สอง เปิดใบลาของบัญชีแรก ต้องเปิดไม่ได้ | ✅ ผ่าน | 12:48 |
| 7 | Firestore Security Rules — บล็อกจริงระดับฐานข้อมูล (ไม่ใช่แค่ UI) ทั้งเทสต์ 5 และ 6 | ✅ ผ่าน | 12:45, 12:48 |

**รวม: ผ่านทั้งหมด 7/7 ข้อ** (ไม่มีข้อที่ไม่ผ่านในรอบนี้)

---

## รายละเอียดแต่ละเทสต์

### 1) `dashboard.html` ต้องเข้าไม่ได้ถ้าไม่ใช่ hr
- **ทดสอบอะไร:** สมัครบัญชีใหม่ (role เริ่มต้น `employee`) แล้วพิมพ์ URL `http://localhost:3002/dashboard.html` เข้าไปตรง ๆ
- **คาดหวัง:** ต้องถูกเด้งไป `index.html` ทันที (หน้านี้เป็นงานของฝ่ายบุคคลเท่านั้นตามสเปก US-11)
- **ผลจริง:** ✅ ผ่าน — เด้งไป `index.html` อัตโนมัติ ไม่เห็นข้อมูลใด ๆ ของแดชบอร์ด
- **โค้ดที่รับผิดชอบ:** [js/dashboard.js:19-22](js/dashboard.js:19)

### 2) `leave-types.html` ต้องเข้าไม่ได้ถ้าไม่ใช่ hr
- **ทดสอบอะไร:** บัญชีเดียวกันจากข้อ 1 พิมพ์ URL `http://localhost:3002/leave-types.html` เข้าไปตรง ๆ
- **คาดหวัง:** ต้องถูกเด้งไป `index.html` ทันที (แม้ปุ่มเพิ่ม/แก้/ลบจะซ่อนอยู่แล้ว แต่ตัวหน้าเองก็ไม่ควรโหลดให้เห็น)
- **ผลจริง:** ✅ ผ่าน — เด้งไป `index.html` อัตโนมัติ
- **โค้ดที่รับผิดชอบ:** [js/leave-types.js:16-19](js/leave-types.js:16)

### 3) Logout แล้วข้อมูลเก่าต้องไม่ค้างจอ
- **ทดสอบอะไร:** ล็อกอินแล้วเปิด `leave-requests.html` ให้ข้อมูลโหลดเสร็จ จากนั้นสั่ง `signOut()` ผ่าน Firebase Auth SDK ตรง (จำลองการออกจากระบบจากแท็บ/อุปกรณ์อื่น) โดยไม่กดปุ่มหรือ navigate เอง
- **คาดหวัง:** หน้าเปลี่ยนไป `login.html` เองภายในไม่กี่วินาที
- **ผลจริง:** ✅ ผ่าน — หน้าเปลี่ยนเป็น `เข้าสู่ระบบ · LeaveEasy` เองภายใน 1 วินาที ไม่ต้อง refresh
- **โค้ดที่รับผิดชอบ:** [js/auth.js:88-101](js/auth.js:88) (listener `onAuthStateChanged` ที่คอยฟังต่อหลังผ่าน `ต้องล็อกอิน()` แล้ว)

### 4) เว้นช่อง "หัวข้อ" ว่างแล้วกดบันทึก
- **ทดสอบอะไร:** ที่หน้า `new-leave-request.html` กรอกทุกช่อง (เหตุผล/ประเภทการลา/วันที่เริ่ม/วันที่สิ้นสุด) **ยกเว้นช่องหัวข้อ** แล้วกดบันทึก
- **คาดหวัง:** ไม่บันทึกลง Firestore และขึ้นข้อความเตือน
- **ผลจริง:** ✅ ผ่าน — ขึ้นข้อความ "⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก" และยืนยันที่ฐานข้อมูลจริงว่าจำนวนใบลาของบัญชีทดสอบ = 0 หลังกดบันทึก
- **โค้ดที่รับผิดชอบ:** [js/new-leave-request.js:97-101](js/new-leave-request.js:97)

### 5) ไม่ล็อกอินแล้วเปิดหน้ารายการ
- **ทดสอบอะไร:** ล็อกเอาต์ให้แน่ใจว่าไม่มีบัญชีค้าง แล้วพิมพ์ URL `http://localhost:3002/leave-requests.html` ตรง ๆ + ยิง `getDocs(collection(db,"leaveRequests"))` ตรงผ่าน Firestore SDK แบบไม่ล็อกอิน (ข้าม UI ไปเลย)
- **คาดหวัง:** เข้าไม่ได้ทั้งสองทาง
- **ผลจริง:** ✅ ผ่าน — (ก) UI เด้งไป `login.html` ทันที (ข) เรียก SDK ตรงได้ผลลัพธ์ `permission-denied`
- **โค้ดที่รับผิดชอบ:** [js/auth.js:91-99](js/auth.js:91) (ชั้น UI) + [firestore.rules:44-46](firestore.rules:44) (ชั้นฐานข้อมูล — เช็ก `request.auth != null`)

### 6) ล็อกอินบัญชีที่สอง เปิดใบลาของบัญชีแรก
- **ทดสอบอะไร:** สร้างบัญชี A ยื่นใบลา 1 ใบ จดรหัสใบลาไว้ (`ztDQrHXqpGkU9jO9opBt`) → sign out → สร้าง/ล็อกอินบัญชี B (คนละ uid) → เปิด `leave-request-detail.html?id=<รหัสใบของ A>` ตรง ๆ + ยิง `getDoc()` ตรงผ่าน SDK ด้วยบัญชี B
- **คาดหวัง:** เปิดไม่ได้ทั้งสองทาง
- **ผลจริง:** ✅ ผ่าน — (ก) UI ขึ้นข้อความ "คุณไม่มีสิทธิ์ดูใบลานี้" ไม่แสดงข้อมูลใด ๆ ของใบลานั้น (ข) เรียก SDK ตรงได้ผลลัพธ์ `permission-denied`
- **โค้ดที่รับผิดชอบ:** [js/leave-request-detail.js:37-44](js/leave-request-detail.js:37) (ชั้น UI — จับเคส `permission-denied` แสดงข้อความที่ถูกต้อง) + [firestore.rules:48-50](firestore.rules:48) (ชั้นฐานข้อมูล — `resource.data.requesterId == request.auth.uid || isApproverOrHr()`)
- **หมายเหตุ:** รอบก่อนหน้า (ก่อนแก้ [js/leave-request-detail.js](js/leave-request-detail.js)) เทสต์นี้ผ่านเช่นกันในแง่ความปลอดภัย (เข้าไม่ได้จริง) แต่ข้อความที่ขึ้นตอนนั้นคือ "⚠️ ยังไม่ได้ตั้งค่า Firebase" ซึ่งสื่อผิด — แก้แล้วและ deploy ขึ้น production hosting เรียบร้อย ยืนยันซ้ำในรอบนี้ว่าขึ้นข้อความถูกต้องแล้ว

### 7) Firestore Security Rules บล็อกจริงระดับฐานข้อมูล
- **ทดสอบอะไร:** รวมผลจากข้อ 5 และ 6 ฝั่ง "ยิง SDK ตรง" — เป็นการยืนยันว่าการป้องกันไม่ได้อยู่แค่ชั้น JavaScript ฝั่งหน้าเว็บ (ซึ่งข้ามได้ง่ายผ่าน console) แต่บังคับจริงที่ Firestore Security Rules
- **คาดหวัง:** `permission-denied` ทุกกรณีที่ไม่ควรเข้าถึงได้
- **ผลจริง:** ✅ ผ่าน — ยืนยันแล้วทั้งกรณีไม่ล็อกอินและกรณีข้าม user

---

## ข้อที่ยังไม่ได้ทดสอบซ้ำในรอบนี้

ย้ายไปอยู่ใน [BACKLOG.md](BACKLOG.md) แล้ว (3 ข้อ — ทั้งหมดคือ "ยังไม่ได้พิสูจน์" ไม่ใช่ "ไม่ผ่าน") เพื่อไม่ให้ปนกับผลเทสต์ 7 ข้อที่ผ่านแล้วจริงด้านบน
