# BACKLOG.md — งานที่ยังไม่ได้ทดสอบ/ยังไม่เสร็จ (ไม่ใช่รายการที่ "ไม่ผ่าน")

> แยกออกมาจาก `test-results.md` เพื่อไม่ให้ปนกับผลเทสต์ที่ผ่านแล้วจริง
> ทุกข้อในไฟล์นี้คือ "ยังไม่ได้พิสูจน์" ไม่ใช่ "พิสูจน์แล้วว่าพัง" — ห้ามอ่านว่าเป็นบั๊ก

| # | รายการ | สถานะ | ต้องทำอะไรก่อนถึงจะทดสอบได้ |
|---|---|---|---|
| 1 | `dashboard.html` ฝั่ง hr แสดงข้อมูลจริงถูกต้อง (ตัวเลขสรุป 3 สถานะ + ใบลาล่าสุด 5 รายการ) | เคยทดสอบผ่านครั้งหนึ่ง (ก่อน Checkpoint 2) แต่ยังไม่ได้ยืนยันซ้ำหลังแก้โค้ดรอบล่าสุด | ต้องมีบัญชีทดสอบ role `hr` — ตั้งค่าผ่าน Firebase Console เท่านั้น (เปลี่ยนเองผ่านแอปไม่ได้แล้วโดยตั้งใจ) |
| 2 | `leave-types.html` ฝั่ง hr เพิ่ม/แก้/ลบประเภทการลาได้จริงภายใต้กฎใหม่ | เคยทดสอบผ่านครั้งหนึ่ง (ก่อน Checkpoint 2) แต่ยังไม่ได้ยืนยันซ้ำหลังแก้โค้ดรอบล่าสุด | เหมือนข้อ 1 — ต้องมีบัญชี hr |
| 3 | ปุ่ม "ให้ AI ช่วยจัดประเภทการลา" กรณีเรียก OpenRouter ไม่สำเร็จ/timeout เกิน 15 วิ | ยังไม่เคยทดสอบจริงเลยตั้งแต่ต้นโปรเจกต์ (มีแค่อ่านโค้ด `AbortController` ประกอบ) | ต้องจำลองการตัดเน็ตหรือบล็อก request ไปยัง OpenRouter ระหว่างทดสอบ |
| 4 | **ย้ายคีย์ AI (OpenRouter) ไปฝั่งที่ผู้ใช้แตะไม่ได้** — ตอนนี้คีย์อยู่ใน `js/openrouter.config.js` ซึ่งรันฝั่ง browser (client-side) เท่านั้น ใครก็เปิด DevTools ดู Network request แล้วเห็นคีย์ได้ทันทีถ้าเว็บที่ deploy จริงมีคีย์ฝังอยู่ | ตอนนี้แก้ปัญหาชั่วคราวด้วยการ **ไม่เอาคีย์ขึ้น production เลย** (ปุ่ม AI บนเว็บจริงเลยกดแล้ว error แบบไม่พังหน้า แต่ใช้งานจริงไม่ได้) | ต้องมี backend คั่นกลาง เช่น Firebase Cloud Functions ที่เก็บคีย์ไว้ฝั่งเซิร์ฟเวอร์ แล้วให้ browser เรียกผ่าน Functions แทนที่จะเรียก OpenRouter ตรง — เป็นงานนอกขอบเขต Module 2 (ไม่มี backend) |

## วิธีปิดงานแต่ละข้อ
- ข้อ 1-2: ขอให้ผู้ใช้ตั้ง role `hr` ให้บัญชีทดสอบผ่าน Firebase Console (Firestore → `users/{uid}` → แก้ field `role`) แล้วแจ้งให้ทดสอบต่อ
- ข้อ 3: ทดสอบโดยปิดอินเทอร์เน็ตชั่วคราวหรือบล็อก request ไปยัง `openrouter.ai` ผ่าน DevTools ระหว่างกดปุ่ม AI แล้วสังเกตว่าไม่ค้าง/ไม่พังหน้าเว็บ
- ข้อ 4: เขียน Cloud Function รับ prompt จาก browser → เรียก OpenRouter ด้วยคีย์ที่เก็บเป็น environment variable ฝั่งเซิร์ฟเวอร์ → ส่งผลลัพธ์กลับ แล้วแก้ `js/openrouter.js` ให้เรียก Function แทน `fetch` ตรงไป OpenRouter
