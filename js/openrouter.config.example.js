// js/openrouter.config.example.js — ตัวอย่างไฟล์ตั้งค่าคีย์ OpenRouter
//
// วิธีใช้: คัดลอกไฟล์นี้เป็นชื่อ "js/openrouter.config.js" (ไฟล์นั้นถูกกันไว้ใน
// .gitignore แล้ว จะไม่ถูก push ขึ้น GitHub) แล้วใส่คีย์จริงของคุณแทนค่าว่างด้านล่าง
export const OPENROUTER_API_KEY = "";
