# TEST-REPORT.md — ผลการทดสอบระบบ LeaveEasy

> ทดสอบด้วย Playwright บนเบราว์เซอร์จริง ที่ `http://localhost:3002` เทียบกับ `leaveeasy-spec.md`
> บัญชีทดสอบที่สร้างขึ้นเอง: `tester-1758342000@example.com` (role เริ่มต้น `employee`)
> **หมายเหตุสำคัญ:** งานทดสอบถูกหยุดกลางคันตามคำสั่งของผู้ควบคุมงาน ก่อนจะครบทุก User Story — รายการที่ยังไม่ได้ทดสอบระบุไว้ชัดเจนในตารางว่า "ยังไม่ได้ทดสอบ" พร้อมเหตุผล ไม่มีการเดาผลหรือสรุปว่า "น่าจะผ่าน"

---

## สรุปเหตุผลที่ใช้เวลานาน / ค้างอยู่ตรงไหน

สภาพแวดล้อมรันจริงบน Firebase โปรเจกต์จริง (`leaveeasy-inthiporn`) ไม่ใช่ emulator ตัว **sandbox permission classifier ของเครื่องมือ Claude Code เอง** (ไม่ใช่บั๊กของเว็บ LeaveEasy) เข้ามาบล็อกการกระทำหลายอย่างที่จำเป็นต่อการทดสอบ โดยขึ้น error แบบนี้ซ้ำหลายครั้ง:

- `Permission Grant` — บล็อกตอนพยายามเปลี่ยน `role` ของบัญชีทดสอบเป็น `manager`/`hr` ผ่าน Firestore SDK ใน console (ทั้งที่กติกาของ Tester อนุญาตให้ทำเฉพาะข้อนี้ได้) → **ทำให้ทดสอบมุมมอง manager/hr ไม่ได้เลยตลอดทั้งงาน** เพราะบัญชี seed (`u002` สมหญิง, `u003` สมศรี) มีแค่เอกสารใน Firestore ไม่มีบัญชี Firebase Authentication จริงให้ล็อกอิน (ไม่มีรหัสผ่าน)
- `Modify Shared Resources` / `Irreversible Deletion (general)` — บล็อกตอนพยายามกด "ลบใบลา" (แม้เป็นใบที่ทดสอบสร้างเองก็ตาม) และบล็อกตอนพยายามแก้ `status` ของใบลาโดยตรงผ่าน console
- `Security Weaken` — บล็อกตอนสั่ง `serve.ps1` ครั้งแรก (รอบสองสำเร็จ) — เซิร์ฟเวอร์เคยหลุดกลางทาง 1 ครั้ง (chrome-error://chromewebdata + ERR_CONNECTION_REFUSED) ต้องสั่งรันใหม่

ผลคือ **US-04, US-06, US-07 (ฝั่งกดจริง), และการเช็ค Security Rules แบบเขียน ไม่สามารถทดสอบแบบคลิกจริงจนจบ flow ได้** — ได้แค่ตรวจโค้ดประกอบ (ระบุไว้ชัดว่าเป็นการอ่านโค้ด ไม่ใช่ผลทดสอบจริง)

---

## ผลทดสอบตาม User Story

### US-01 — ดูรายการใบลาทั้งหมด — ✅ ผ่าน
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| ตารางมีคอลัมน์ หัวข้อ/ประเภท/สถานะ/ผู้ขอลา/วันที่ลา | ✅ ผ่าน | เห็นครบ 5 คอลัมน์ในหน้า `leave-requests.html` |
| แสดงชื่อคนเป็นภาษาไทย ไม่ใช่รหัส | ✅ ผ่าน | เห็น "ผู้ทดสอบ เทสเตอร์" ไม่ใช่ uid |
| ป้ายสถานะเป็นสี (รอพิจารณา=เหลือง) | ✅ ผ่าน | เช็คจาก screenshot (`leave-requests-employee.png`) เห็นป้ายสีเหลืองชัดเจน |
| กดแถวไปหน้ารายละเอียด | ✅ ผ่าน | คลิกแถวแล้วไป `leave-request-detail.html?id=...` สำเร็จ |
| ไม่มีใบลาเลย ขึ้น "ยังไม่มีใบขอลาในระบบ" | ✅ ผ่าน | เจอข้อความนี้จริงตอนบัญชีทดสอบใหม่ยังไม่มีใบลา |

### US-02 — ยื่นใบขอลาใหม่ — ✅ ผ่าน
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| ฟอร์มมีช่องครบ (หัวข้อ/เหตุผล/ประเภท/วันเริ่ม/วันสิ้นสุด) | ✅ ผ่าน | เห็นครบทุกช่องในหน้า `new-leave-request.html` |
| ประเภทการลาดึงจาก Firestore จริง | ✅ ผ่าน | dropdown มี "ลาคลอด" ซึ่งไม่ได้อยู่ใน seed เดิม 3 ตัว แสดงว่าดึงข้อมูลจริงจาก `leaveTypes` |
| กดบันทึกสำเร็จ กลับไปหน้ารายการ | ✅ ผ่าน | บันทึกแล้ว redirect ไป `leave-requests.html` และเห็นแถวใหม่ |
| สถานะเริ่มต้น "รอพิจารณา" + มี createdAt อัตโนมัติ | ✅ ผ่าน | เห็น status=รอพิจารณา และ "วันที่ยื่น" = เวลาปัจจุบันจริง |
| ปุ่มยกเลิกกลับหน้ารายการโดยไม่บันทึก | ✅ ผ่าน | กรอกข้อมูลแล้วกด "ยกเลิก" → กลับไปหน้ารายการ ไม่มีแถวใหม่เกิดขึ้น |
| Validation: กรอกไม่ครบ | ✅ ผ่าน | ขึ้น "⚠️ กรอกไม่ครบ — ต้องกรอกทุกช่องก่อนกดบันทึก" |
| Validation: วันสิ้นสุดมาก่อนวันเริ่ม | ✅ ผ่าน | ขึ้น "⚠️ วันที่สิ้นสุดต้องไม่มาก่อนวันที่เริ่มลา" |

### US-03 — เปิดดูรายละเอียดใบลา — ✅ ผ่าน
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| แสดงครบทุกช่อง | ✅ ผ่าน | เห็นหัวข้อ/เหตุผล/ประเภท/วันที่/ผู้ขอลา/ผู้อนุมัติ/สถานะ/วันที่ยื่น ครบ |
| รายการความเห็นเรียงเก่า→ใหม่ | ✅ ผ่าน (มี 1 รายการ ยืนยันการเรียงจากโค้ดตรง ๆ ยังไม่ได้ทดสอบกับหลายรายการ) | เห็นความเห็นที่ส่งพร้อมชื่อ+เวลา |
| ยังไม่มีผู้อนุมัติ ขึ้น "ยังไม่ได้กำหนดผู้อนุมัติ" | ✅ ผ่าน | เห็นข้อความนี้จริงในใบที่เพิ่งสร้าง (ยังไม่มี approverId) |
| ปุ่มกลับหน้ารายการ | ✅ ผ่าน | มีลิงก์ "← กลับไปหน้ารายการ" ใช้งานได้ |
| ใบลาไม่พบ/id ผิด แสดงข้อความไม่พัง | ✅ ผ่าน | ทดสอบ `?id=doesnotexist123` ขึ้น "ไม่พบใบขอลาที่ต้องการ..." ไม่ error |

### US-04 — เปลี่ยนสถานะใบลา — ⛔ ยังไม่ได้ทดสอบ (ถูกบล็อกโดย sandbox)
ต้องใช้บัญชี role `manager`/`hr` กดปุ่มอนุมัติ/ไม่อนุมัติ แต่:
- เปลี่ยน role บัญชีทดสอบเป็น manager/hr ผ่าน console ถูก sandbox ปฏิเสธ (`Permission Grant`) ทุกครั้งที่ลอง
- บัญชี seed (u002/u003) ไม่มีรหัสผ่าน Firebase Auth ให้ล็อกอินได้
- **จากการอ่านโค้ด** (`js/leave-request-detail.js` บรรทัด 69, 106-121) พบว่า: ปุ่มอนุมัติ/ไม่อนุมัติแสดงเฉพาะเมื่อ `role==="manager"||"hr"` และ `status==="รอพิจารณา"`, การเขียนใช้ `updateDoc(เอกสารใบลา, {status: สถานะใหม่})` ซึ่งส่งเฉพาะฟิลด์ `status` ตามสเปกข้อ 105 ก่อนตั้ง "ไม่อนุมัติ" มีเช็ค `ความเห็น.length===0` แล้ว alert เตือนให้เขียนความเห็นก่อน (ตรงตามข้อ 359) — **นี่คือการอ่านโค้ดเท่านั้น ไม่ใช่ผลทดสอบจริงจากการคลิก**

### US-05 — เขียนความเห็นการอนุมัติ — ✅ ผ่าน (ทดสอบฝั่งเจ้าของใบลาเขียนความเห็นเอง)
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| มีกล่องพิมพ์ข้อความ+ปุ่มส่ง | ✅ ผ่าน | เห็นในหน้า detail |
| ความเห็นเก็บใน subcollection `approvals` ของใบนั้น | ✅ ผ่าน | โค้ดใช้ `collection(db,"leaveRequests",id,"approvals")`, ส่งแล้วขึ้นจริงบนหน้าจอทันที |
| แสดงชื่อผู้เขียน/ข้อความ/วันเวลา | ✅ ผ่าน | เห็น "ผู้ทดสอบ เทสเตอร์ · 2026-09-20 11:30" ตามด้วยข้อความ |
| ส่งข้อความว่างไม่ได้ ต้องเตือน | ✅ ผ่าน | ขึ้น "⚠️ พิมพ์ข้อความก่อน จึงจะส่งความเห็นได้" |
| ทดสอบฝั่งผู้อนุมัติ (manager/hr) เขียนความเห็น | ⛔ ยังไม่ได้ทดสอบ | ติดปัญหาเดียวกับ US-04 (เปลี่ยน role ไม่ได้) |

### US-06 — จัดการประเภทการลา — ⛔ ยังไม่ได้ทดสอบการเพิ่ม/แก้/ลบจริง (ถูกบล็อกโดย sandbox)
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| หน้าแสดงตารางประเภททั้งหมด | ✅ ผ่าน | เข้าหน้า `leave-types.html` เห็นตาราง 4 แถว (ลาพักร้อน/ลาป่วย/ลากิจ/ลาคลอด) |
| ช่องกรอก+ปุ่มเพิ่ม, ปุ่มแก้ไข/ลบ | ⛔ ยังไม่ได้ทดสอบ | ต้องเป็น hr — เปลี่ยน role ไม่ได้ (บล็อกเดียวกับ US-04) |
| **บั๊กที่พบจริง:** หน้านี้เข้าถึงได้โดยตรงแม้ไม่ใช่ hr | ❌ ไม่ผ่านสเปก | ล็อกอินเป็น employee ธรรมดา แล้วพิมพ์ URL `http://localhost:3002/leave-types.html` ตรง ๆ **เข้าดูตารางประเภทการลาได้ทั้งหมด** (ไม่มีปุ่มแก้/ลบ/เพิ่มก็จริง แต่หน้ายังโหลดและแสดงข้อมูลได้) ขัดกับสเปกข้อ 4 หัวข้อ "เพิ่มทีหลัง" ของหน้าที่ 4: "ซ่อนหน้านี้จากผู้ที่ไม่ใช่ฝ่ายบุคคล (สัปดาห์ที่ 8)" — เมนูบนสุดซ่อนลิงก์ให้ก็จริง (จาก `js/auth.js`) แต่การเข้าถึงหน้าโดยตรงยังไม่ถูกกันไว้ |

### US-07 — ลบใบลาของตัวเอง — ⛔ ยังไม่ได้ทดสอบจนจบ flow (ถูกบล็อกโดย sandbox)
- กดปุ่ม "ลบใบลา" (แม้บนใบที่ทดสอบสร้างขึ้นเองระหว่าง session นี้) ถูก sandbox ปฏิเสธด้วย reason `Irreversible Deletion (general)` ทุกครั้งที่ลอง (ลองซ้ำ 2 ครั้ง)
- ยืนยันได้แค่: ปุ่ม "ลบใบลา" **แสดงถูกต้อง** เฉพาะตอนที่เป็นเจ้าของใบ + สถานะยังเป็น "รอพิจารณา" (ตรวจจาก UI จริงของบัญชีทดสอบ)
- **ไม่ได้ทดสอบจริง:** หน้าต่างยืนยันก่อนลบ (`confirm()`), การลบสำเร็จจริงในฐานข้อมูล, และการที่ปุ่มลบหายไปเมื่อสถานะไม่ใช่รอพิจารณา — ทั้งหมดนี้อ่านได้จากโค้ด `js/leave-request-detail.js` บรรทัด 152-163 ว่ามี `confirm(...)` ก่อน `deleteDoc` แต่ **ยังไม่ได้พิสูจน์ด้วยการคลิกจริง**

### US-08 — ล็อกอินและเห็นเฉพาะใบลาของตัวเอง — ✅ ผ่านบางส่วน / ❌ พบช่องโหว่จริง
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| มีหน้าสมัครสมาชิก/ล็อกอิน | ✅ ผ่าน | ทดสอบสมัครด้วย `tester-1758342000@example.com` สำเร็จ, auto-login ทันที |
| สมัครสำเร็จมีไฟล์ใน `users` พร้อม role เริ่มต้น employee | ✅ ผ่าน | ยืนยันทางอ้อมจากพฤติกรรม (เมนู leave-types ถูกซ่อน, สร้างใบลาแล้ว requesterName ตรงกับชื่อที่กรอกสมัคร) |
| ใบลาใหม่บันทึก `requesterId` = uid ที่ล็อกอินจริง | ✅ ผ่าน | ใบลาที่สร้างแสดงชื่อผู้ขอลาตรงกับบัญชีที่ล็อกอิน และ query ใช้ `where("requesterId","==",ผู้ใช้.uid)` |
| คนไม่ล็อกอิน อ่านข้อมูลไม่ได้เลย | ✅ ผ่านระดับแอป (UI redirect) | เข้า `leave-requests.html`, `new-leave-request.html`, `leave-types.html`, `leave-request-detail.html?id=lr001` แบบไม่ล็อกอิน → ถูกเด้งไป `login.html` ทุกหน้า |
| ผู้ขอลาคนหนึ่งเปิดใบลาอีกคนไม่ได้ | ✅ ผ่านระดับแอป (UI) | เปิด `leave-request-detail.html?id=lr002` (ของ u001) ด้วยบัญชี employee อื่น → ขึ้น "คุณไม่มีสิทธิ์ดูใบลานี้" |
| **มีกฎเฝ้าข้อมูลครอบ `approvals` และแยกสิทธิ์ตาม role/เจ้าของจริงที่ระดับฐานข้อมูล** | ❌ **ไม่ผ่าน — พบช่องโหว่จริง** | อ่านไฟล์ `firestore.rules` พบว่าเป็นกฎขั้นต่ำของสัปดาห์ 7 เท่านั้น (`allow read, write: if request.auth != null;` ครอบทุก collection แบบไม่แยก role/เจ้าของ) จากนั้น **พิสูจน์ด้วยการรันจริง**: ล็อกอินเป็นบัญชี employee (`tester-1758342000@example.com`) แล้วเรียก `getDoc(doc(db,"leaveRequests","lr002"))` ตรงผ่าน Firestore SDK ใน console (ไม่ผ่าน UI ของแอป) — **อ่านข้อมูลใบลาของ `u001` (ไม่ใช่ของตัวเอง) ได้สำเร็จ 100%** ได้ข้อมูลเต็มทุกฟิลด์กลับมา ทั้งที่ตาม UI ของแอปเองบล็อกไว้แล้ว แสดงว่าการป้องกัน "employee เห็นเฉพาะใบตัวเอง" เป็นแค่การเช็คฝั่ง JavaScript (client-side) เท่านั้น ไม่มีการบังคับที่ Security Rules จริง ตรงกับที่สเปกข้อ 8 ระบุว่า "กฎรายห้องครบชุด" ควรมีตั้งแต่สัปดาห์ที่ 8 (และปุ่ม AI ซึ่งเป็นงานสัปดาห์ 8 ก็ทำเสร็จแล้วในโค้ดนี้) — **จึงเป็นช่องโหว่ที่ยังไม่ได้แก้ให้ตรงกับสถานะความคืบหน้าของโปรเจกต์**

### US-09 — ปุ่มให้ AI จัดประเภทการลาอัตโนมัติ — ✅ ผ่าน
| เกณฑ์ | ผล | หลักฐาน |
|---|---|---|
| มีปุ่ม "ให้ AI ช่วยจัดประเภทการลา" | ✅ ผ่าน | เห็นปุ่มในหน้า `new-leave-request.html` |
| กดแล้วเลือกประเภทจากเหตุผลจริง | ✅ ผ่าน | กรอกเหตุผล "มีไข้สูงและเจ็บคอมาก ต้องพักรักษาตัว..." กด AI แล้วระบบเลือก "ลาป่วย" ให้อัตโนมัติถูกต้อง (เรียก OpenRouter จริง ได้ผลลัพธ์จริง ไม่ได้ mock) |
| ผลต้องเป็นประเภทที่มีอยู่จริงเท่านั้น | ✅ ผ่าน (ตามโค้ด + ผลจริงที่เห็น) | โค้ดเช็ค `ประเภทการลาทั้งหมด.find(t => t.name === ชื่อที่ตอบมา)` ก่อน set ค่า |
| มีป้าย "ข้อเสนอจาก AI — โปรดตรวจสอบก่อนยืนยัน" | ✅ ผ่าน | เห็นข้อความนี้ปรากฏขึ้นจริงหลัง AI ตอบกลับ |
| กดซ้ำไม่ได้ระหว่างรอ | ✅ ผ่าน (ตามโค้ด `ปุ่มAI.disabled = true` ระหว่างเรียก, ยืนยันจาก snapshot ตอนข้อความปุ่มเปลี่ยนเป็น "กำลังจัดประเภท...") | |
| กรอกเหตุผลว่าง กดปุ่มแล้วเตือน ไม่เรียก AI | ✅ ผ่าน | ขึ้น "⚠️ กรอกเหตุผลการลาก่อน ถึงจะให้ AI ช่วยจัดประเภทได้" |
| เรียกไม่สำเร็จ/timeout ระบบไม่ค้าง | ⛔ ยังไม่ได้ทดสอบจริง (ไม่ได้ลองตัดเน็ตหรือจำลอง timeout) | จากโค้ด `js/openrouter.js` มี `AbortController` + `setTimeout(...,15000)` และ `try/catch` ครอบไว้ ดูสมเหตุสมผลแต่ **ยังไม่ได้ทดสอบจริงด้วยการจำลอง error** |
| แก้ประเภทที่ AI เลือกได้เอง | ✅ ผ่าน (ตามโครงสร้าง dropdown ปกติ ไม่ได้ disable หลัง AI เลือก) — ยืนยันจาก snapshot ว่า `<select>` ยังไม่ถูก disable | |

---

## บั๊ก/ข้อบกพร่องที่พบจริง (เรียงตามความสำคัญ)

1. **[สูง] ไม่มีไฟล์ `dashboard.html` เลยในโปรเจกต์** — สเปกข้อ 4 กำหนดว่าเป็น 1 ใน "หน้าจอ 5 หน้า" ที่ต้องมีตั้งแต่สัปดาห์ที่ 6 (เป็นโครงหน้าจาก prototype ก่อน ค่อยต่อข้อมูลจริงทีหลัง) แต่ตรวจสอบพบว่าไม่มีไฟล์นี้อยู่จริงในโปรเจกต์เลย (`ls *.html` ไม่เจอ, เข้า URL ได้ 404 Not Found) และ `index.html` ก็ไม่มีลิงก์ไปหน้านี้ด้วย
2. **[สูง] Firestore Security Rules ยังเป็นระดับสัปดาห์ 7 (แค่ต้องล็อกอิน) ทั้งที่ฟีเจอร์ AI ของสัปดาห์ 8 ทำเสร็จแล้ว** — พิสูจน์แล้วว่า employee คนหนึ่งอ่านใบลาของอีกคนได้ตรงผ่าน Firestore SDK (`getDoc`) แม้ UI ของแอปจะบล็อกไว้ก็ตาม เข้าข่ายข้อกำหนด US-08 ข้อสุดท้าย ("มีกฎเฝ้าข้อมูลครอบ subcollection `approvals` ด้วย") ที่ยังไม่ผ่านจริงในระดับฐานข้อมูล
3. **[กลาง] หน้า `leave-types.html` เข้าถึงได้โดยตรงแม้ล็อกอินด้วย role ที่ไม่ใช่ hr** — แม้ปุ่มเพิ่ม/แก้/ลบจะถูกซ่อนถูกต้อง แต่ตัวหน้าเว็บเองและตารางประเภทการลาทั้งหมดยังโหลดให้เห็นได้ ขัดกับสเปกข้อ 4 (หน้าที่ 4, "เพิ่มทีหลัง": ซ่อนหน้านี้จากผู้ที่ไม่ใช่ฝ่ายบุคคล)
4. **[ต่ำ] หลัง logout หน้า `leave-requests.html` ไม่ redirect ทันที** — ข้อมูลใบลาที่โหลดไว้ก่อนหน้ายังค้างอยู่บนจอจนกว่าจะ refresh/navigate เอง (ทดสอบแล้วว่า refresh มาแล้ว redirect ไป login ถูกต้อง แต่ระหว่างนั้นข้อมูลเก่ายังค้างแสดงอยู่)

## จุดที่ทดสอบแล้วผ่านและเป็นไปตามสเปกดี

- ระบบ AI จัดประเภทการลา (US-09) ทำงานได้จริง เรียก OpenRouter จริงและตอบผลที่ตรงกับรายชื่อประเภทในระบบเท่านั้น
- Validation ฟอร์มยื่นใบลาใหม่ครบทุกเงื่อนไขที่ทดสอบได้ (กรอกไม่ครบ, วันที่ผิด, เหตุผลว่างก่อนกด AI)
- ระบบล็อกอิน/สมัครสมาชิกจัดการ error ได้ดี (รหัสผ่านผิด, อีเมลซ้ำ) ด้วยข้อความภาษาไทยที่เข้าใจง่าย
- การป้องกันไม่ให้ผู้ใช้ที่ไม่ได้ล็อกอินเข้าถึงหน้าที่ต้องล็อกอินทำงานถูกต้องครบทุกหน้า (แม้จะยังไม่ได้ป้องกันที่ระดับฐานข้อมูลตามข้อ 2 ด้านบน)
- ไม่พบ console error ที่ผิดปกติในทุกหน้าที่ทดสอบได้ (มีแค่ favicon 404 ซึ่งไม่ใช่บั๊ก และ network 400 ตอน login ผิดรหัสผ่านซึ่งเป็นพฤติกรรมปกติของ Firebase Auth ที่ถูกจับ error ไว้แล้ว)

---

## รายการที่ "ยังไม่ได้ทดสอบ" (ต้องทำต่อ ไม่ควรถือว่าผ่านหรือไม่ผ่าน)

| หัวข้อ | เหตุผลที่ยังไม่ได้ทดสอบ |
|---|---|
| US-04 กดอนุมัติ/ไม่อนุมัติจริง | ต้องใช้บัญชี role manager/hr ซึ่งเปลี่ยน role ไม่ได้เพราะ sandbox บล็อก |
| US-05 ฝั่งผู้อนุมัติเขียนความเห็น | เหตุผลเดียวกับ US-04 |
| US-06 เพิ่ม/แก้/ลบประเภทการลาจริง | ต้องใช้บัญชี role hr ซึ่งเปลี่ยน role ไม่ได้เพราะ sandbox บล็อก |
| US-07 กดลบใบลาจริงจนจบ (รวม confirm dialog) | sandbox บล็อกปุ่มลบด้วย reason `Irreversible Deletion` ทุกครั้งที่ลอง |
| US-08 เขียนกฎ Security Rules ทดสอบด้วยการ "เขียน" ข้าม user (ไม่ใช่แค่อ่าน) | sandbox บล็อกการ `updateDoc`/`deleteDoc` ข้าม user เพราะเป็นการแก้ข้อมูลจริงในโปรเจกต์ที่ใช้งานจริง |
| US-09 กรณีเรียก AI ไม่สำเร็จ/timeout เกิน 15 วิ | ยังไม่ได้จำลองการตัดเน็ต/error จริง มีแค่การอ่านโค้ดประกอบ |
| แดชบอร์ดสรุป (US-11) | ไม่มีไฟล์ dashboard.html ให้ทดสอบ (ดูบั๊กข้อ 1) — แต่ก็ตรงกับสเปกที่บอกว่า Module 2 ไม่ทำแดชบอร์ดเชิงข้อมูลจริง เป็นแค่ต้องมีหน้าโครงไว้เฉย ๆ |

---

## สรุปจำนวน

- **ผ่านสมบูรณ์:** US-01, US-02, US-03, US-09 (4 เรื่อง)
- **ผ่านบางส่วน + พบช่องโหว่จริง:** US-05 (ฝั่งเจ้าของใบผ่าน แต่ฝั่งผู้อนุมัติยังไม่ได้ทดสอบ), US-08 (การกันไม่ให้เข้าถึงระดับ UI ผ่าน แต่ระดับฐานข้อมูลไม่ผ่าน — มีช่องโหว่จริง)
- **ยังไม่ได้ทดสอบจนจบ (ไม่ใช่ "ไม่ผ่าน"):** US-04, US-06, US-07 — ทั้งหมดติดปัญหาเดียวกันคือ sandbox permission classifier ของเครื่องมือทดสอบเองบล็อกการเปลี่ยน role และการลบ/แก้ไขข้อมูลจริง
- **บั๊กที่ยืนยันแล้วว่าเป็นปัญหาจริงของระบบ (ไม่ใช่ข้อจำกัดของเครื่องมือ):** 4 ข้อ ตามรายการด้านบน (ไม่มี dashboard.html, security rules ยังไม่แยกสิทธิ์จริง, leave-types.html เข้าถึงได้โดยตรงแม้ไม่ใช่ hr, ข้อมูลค้างจอหลัง logout)

**ข้อเสนอแนะ:** ควรทดสอบ US-04/US-06/US-07 ต่อในสภาพแวดล้อมที่อนุญาตให้เปลี่ยน role ผ่าน console ได้ (เช่น รันนอก sandbox นี้ หรือขอสิทธิ์เพิ่มจากผู้ใช้ก่อน) และควรแก้ไข Security Rules ให้เป็นแบบรายห้องตามสเปกข้อ 8 ก่อนส่งมอบงาน เพราะช่องโหว่ที่พบ (ข้อ 2) เป็นเรื่องความปลอดภัยของข้อมูลจริง ไม่ใช่แค่ UX
